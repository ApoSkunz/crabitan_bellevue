name: Security

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: '0 6 * * 1'

jobs:
  trufflehog:
    name: TruffleHog Secret Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: TruffleHog
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: ${{ github.event.pull_request.base.sha || github.event.before }}
          head: ${{ github.event.pull_request.head.sha || github.sha }}
          extra_args: --only-verified

  codeql:
    name: CodeQL Analysis
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: javascript

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3

  exakat:
    name: Exakat PHP Analysis
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'

      - name: Download Exakat
        run: |
          curl -o exakat.phar https://www.exakat.io/versions/exakat.phar
          php exakat.phar version

      - name: Init project
        run: |
          php exakat.phar init -p project -R ${{ github.workspace }} 2>&1
          echo "--- workspace ---" && ls -la ${{ github.workspace }}
          echo "--- search projects dir in workspace ---"
          find ${{ github.workspace }} -maxdepth 4 -name "projects" -type d 2>/dev/null || true
          echo "--- search projects dir in HOME ---"
          find $HOME -maxdepth 4 -name "projects" -type d 2>/dev/null || true

      - name: Run analysis
        run: php exakat.phar project -p project 2>&1

      - name: Generate report
        run: |
          php exakat.phar report -p project -format Text 2>&1
          echo "--- all files created by exakat ---"
          find ${{ github.workspace }} -maxdepth 5 -newer exakat.phar -type f 2>/dev/null || true
          find $HOME -maxdepth 5 -newer exakat.phar -type f 2>/dev/null || true

      - name: Upload Exakat Report
        uses: actions/upload-artifact@v4
        with:
          name: exakat-report
          path: projects/
          if-no-files-found: warn

  sast:
    name: SAST — Semgrep
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Semgrep
        uses: semgrep/semgrep-action@v1
        with:
          config: >-
            p/php
            p/owasp-top-ten
            p/security-audit

  legitify:
    name: Legitify GitHub Security
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Legitify
        uses: Legit-Labs/legitify@main
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          target: REPOSITORY
          scorecard: true
